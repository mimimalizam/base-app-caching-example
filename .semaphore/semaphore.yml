version: "v1.0"
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Cache Ruby installation"
    task:
      jobs:
      - name: Cache Ruby
        commands:
          - export ruby_version="2.3.1" && export gem_version="2.6.14"
          - export ruby_archive="$ruby_version.tar.gz"
          - export ruby_install_path="~/.rbenv/versions/$ruby_version"
          - >
            if [ -e tmp ]; then
              echo "It is here"
            fi
