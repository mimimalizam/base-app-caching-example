version: v1.0
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Cache: skip verification"
    task:
      prologue:
        commands:
          - rm -rf ~/.toolbox || true
          - git clone -b lr/cache-absolute-path  --single-branch https://github.com/semaphoreci/toolbox.git ~/.toolbox
          - bash ~/.toolbox/install-toolbox
          - source ~/.toolbox/toolbox
          - echo 'source ~/.toolbox/toolbox' >> ~/.bash_profile
          - rm -rf ~/.ssh/known_hosts
          - pkill ssh-agent
          - ls -lah
          - checkout
          - ls -lah
      jobs:
      - name: Cache Store and Restore regular path
        commands:
          - cache list
          - cache usage
          - dd if=/dev/zero of=tmp.file bs=1M count=50
          - cache store tmp-archive-$SEMAPHORE_JOB_ID tmp.file
          - git status
          - cache store tmp-archive-$SEMAPHORE_JOB_ID tmp.file && rm -f tmp.file
          - git status && ls -lah
          - cache restore tmp-archive-$SEMAPHORE_JOB_ID
          - git status && ls -lah
  - name: "Absolute paths check"
    task:
      prologue:
        commands:
          - rm -rf ~/.toolbox || true
          - git clone -b lr/cache-absolute-path  --single-branch https://github.com/semaphoreci/toolbox.git ~/.toolbox
          - bash ~/.toolbox/install-toolbox
          - source ~/.toolbox/toolbox
          - echo 'source ~/.toolbox/toolbox' >> ~/.bash_profile
          - rm -rf ~/.ssh/known_hosts
          - pkill ssh-agent
          - ls -lah
          - checkout
          - ls -lah
      jobs:
      - name: Cache Store and Restore /home/something
        commands:
          - cache usage
          - dd if=/dev/zero of=tmp.file bs=1M count=50
          - sudo mkdir -p /var/to-cache && sudo mv tmp.file /var/to-cache/
          - cache store from-var-$SEMAPHORE_JOB_ID /var/to-cache/tmp.file
          - sudo rm -rf /var/to-cache
          - ls -lah /var
          - cache restore from-var-$SEMAPHORE_JOB_ID
          - ls -lah /var
